# Setup
- Description: ROS2 gazbeo simulation for open manipulator arm

Download the repository into `<ros workspace>/src` and use `colcon build` to build

# Commands to launch
`ros2 launch open_manipulator_gazebo open_manipulator_x.launch.py`
